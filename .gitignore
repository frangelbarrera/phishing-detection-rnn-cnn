# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# PDFs
*.pdf

# Local-only artifacts (do not commit; regenerate via train.py)
# - dataset_phishing.csv is committed by intention (small enough, reproducibility)
# - trained artifacts (my_model.keras, scaler.pkl, etc.) ARE committed as the
#   canonical inference artifacts. If you retrain locally, you may keep or
#   overwrite them; just verify metrics.json matches before pushing.
*.npy
